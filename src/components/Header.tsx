import React from 'react';
import { Box, Typography } from '@mui/material';

const Header: React.FC = () => (
  <Box mt={4} mb={4}>
    <Typography variant="h4" align="center">
      Personal Reminder Dashboard - Zen Mode
    </Typography>
  </Box>
);

export default Header;
